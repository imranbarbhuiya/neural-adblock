import { classifyRequest } from "../src/index"; const requests = ["https://ads.vendor.test/ads/banner.js", "https://analytics.vendor.test/collect/event", "https://static.example.test/assets/app.js"]; for (let i = 0; i < 1000; i++) classifyRequest(requests[i % 3]); const iterations = 100000, start = performance.now(); for (let i = 0; i < iterations; i++) classifyRequest(requests[i % 3]); const elapsedMs = performance.now() - start, result = { iterations, elapsedMs, requestsPerSecond: iterations / (elapsedMs / 1000), microsecondsPerRequest: elapsedMs * 1000 / iterations, runtime: `Bun ${Bun.version}`, platform: `${process.platform}-${process.arch}` }; await Bun.write(new URL("../benchmarks/latest.json", import.meta.url), `${JSON.stringify(result, null, 2)}\n`); console.log(JSON.stringify(result, null, 2));
